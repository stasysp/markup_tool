# Automatic Video Markup Tool

## Installation
- Install compiller
```Shell
sudo apt-get update
sudo apt-get install g++
```
- Build app
```Shell
git clone https://github.com/stasysp/markup_tool
mkdir build-dir 
cd build-dir
cmake ..
make
```

